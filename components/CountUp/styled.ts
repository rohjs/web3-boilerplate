import styled from 'styled-components'

export const StyledCountUp = styled.strong`
  white-space: nowrap;
  font-size: inherit;
  font-weight: inherit;
  color: inherit;
  letter-spacing: inherit;
  line-height: inherit;
  transition: 150ms;

  .symbol {
    margin-left: 0.25em;
  }
`
