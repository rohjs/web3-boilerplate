export enum ConnectorNames {
  BSC = "bsc",
  CoinbaseWallet = "coinbaseWallet",
  Injected = "injected",
  MetaMask = "metaMask",
  WalletConnect = "walletConnect",
}

export enum Network {
  MAINNET = 1,
  GOERLI = 5,
  GÖRLI = 5,
  BSC = 56,
  BSC_TESTNET = 97,
}
